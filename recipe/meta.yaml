{% set name = "bayesbay" %}
{% set version = "0.3.12" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/bayesbay-{{ version }}.tar.gz
  sha256: 21e4e13982ea1e83b2c3a9efcc6aec73ea9152995bd6b91e9e4c771caa42eb90

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - python
    - setuptools
    - wheel
    - cython
    - numpy
    - pip
  run:
    - python
    - scipy >=1.9.0
    - matplotlib-base >=3.0.0
    - shapely >=2.0.0
    - joblib >=1.3.2

test:
  imports:
    - bayesbay
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/fmagrini/bayes-bay
  summary: 'Generalized trans-dimensional and hierarchical MCMC in Python'
  description: |
    BayesBay is a user-friendly Python package for generalized trans-dimensional and hierarchical MCMC sampling.
  license: BSD-3-Clause
  license_file: LICENCE
  doc_url: https://bayes-bay.readthedocs.io/
  dev_url: https://github.com/fmagrini/bayes-bay

extra:
  recipe-maintainers:
    - fmagrini
    - jwhhh
